import 'package:flutter/material.dart';

class NotesMobile extends StatelessWidget {
  const NotesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('This is Notes mobile')));
  }
}
