import 'package:flutter/material.dart';

class NotesSharedWithMe extends StatelessWidget {
  const NotesSharedWithMe({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is notes shared with me page'));
  }
}
