import 'package:flutter/material.dart';

class NotesDeleted extends StatelessWidget {
  const NotesDeleted({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is a deleted note page'));
  }
}
