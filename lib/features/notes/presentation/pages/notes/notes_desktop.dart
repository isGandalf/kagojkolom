import 'package:flutter/material.dart';

class NotesDesktop extends StatelessWidget {
  const NotesDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('This is Notes desktop')));
  }
}
