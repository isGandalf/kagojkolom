import 'package:flutter/material.dart';

class TrashNotes extends StatelessWidget {
  const TrashNotes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is trash notes'));
  }
}
