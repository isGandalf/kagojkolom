import 'package:flutter/material.dart';

class CalendarWidgetPage extends StatelessWidget {
  const CalendarWidgetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is calendar'));
  }
}
