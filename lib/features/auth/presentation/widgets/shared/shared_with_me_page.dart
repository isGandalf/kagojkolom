import 'package:flutter/material.dart';

class SharedWithMePage extends StatelessWidget {
  const SharedWithMePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is Share with me'));
  }
}
